---
inclusion: always
---

# Shell Environment

- ALWAYS use WSL2 zsh/bash commands
- NEVER use PowerShell or cmd syntax
- Use Unix syntax for ALL commands
- Forward slashes in all paths
- When running shell commands, prefix with `wsl` or use bash -c syntax to ensure they run in the Linux environment
